\begin{deluxetable*}{lrrrrrrr}
\tablecaption{ {{ title }} }
\tablehead{
{% for h in headers -%}
\colhead{ {{ h }} }{% if not loop.last %} & {% endif %}
{%- endfor %}
}
\startdata
{% for row in rows -%}
{{ row | join(' & ') }} \\
{%- endfor %}
\enddata
\end{deluxetable*}